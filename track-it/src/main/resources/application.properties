spring.application.name=trackIt

# --- MySQL Configuration ---
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/trackit?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Shiv@1248
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA Settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# --- Server Settings ---
server.port=8080

# --- Security Settings ---
spring.security.user.password=
jwt.secret=your-secret-key-here-make-it-long-and-secure-in-production
jwt.expiration=3600000

# --- CORS Configuration ---
spring.web.cors.allowed-origins=http://localhost:4200
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# --- Logging ---
logging.level.com.shivansh.trackIt=DEBUG
logging.level.org.springframework.security=DEBUG
